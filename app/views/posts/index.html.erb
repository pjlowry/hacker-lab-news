<h1>Hacker Lab News</h1>

<h2>A user-contributed Posting site.</h2>

<table>
  <tr>
    <td>Headline</td>
    <td>Source URL</td>
  </tr>
  <% @posts.each do |post| %>
    <tr>
      <td><%= post.headline %></td>
      <td><%= post.source_url %></td>
      <td><%= link_to 'Delete', post, :method => :delete, :data => { :confirm => 'Are you sure?' } %></td>
    </tr>
  <% end %>
</table>

<p><%= link_to 'Add Post', new_post_path %></p>